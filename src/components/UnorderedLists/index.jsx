export default function UnorderedList({children}) {
	/**
	 * Unordered List Template
	 * 	- reveices li elements from parent
	 * 	- doesn't know what it's rendering
	 *  -
	 */

	return <ul>{children}</ul>;
}
